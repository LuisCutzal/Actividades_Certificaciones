<div class="container mt-5">

    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h3 class="fw-semibold mb-0">
            Resultados de búsqueda
        </h3>

        <a href="/admin/listarUsuario"
            class="btn btn-sm btn-outline-secondary">
            ← Volver al listado
        </a>
    </div>

    <!-- Subtítulo -->
    <p class="text-muted mb-4">
        Coincidencias para:
        <span class="fw-semibold text-primary">
            “<?= $this->escapeHtml($query) ?>”
        </span>
    </p>

    <?php if (empty($usuarios)): ?>
        <div class="alert alert-light text-center border">
            <span class="text-muted">
                No se encontraron usuarios con ese criterio.
            </span>
        </div>
    <?php else: ?>

        <!-- Tabla -->
        <div class="card shadow-sm border-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-light small text-uppercase">
                        <tr>
                            <th>Usuario</th>
                            <th>Correo</th>
                            <th>CUI</th>
                            <th>Rol</th>
                            <th>Estado en el sistema</th>
                            <th class="text-center">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($usuarios as $u): ?>
                            <tr>
                                <td class="fw-medium">
                                    <?= $this->escapeHtml($u['nombre'] . ' ' . $u['apellido']) ?>
                                </td>
                                <td class="text-muted">
                                    <?= $this->escapeHtml($u['correo']) ?>
                                </td>
                                <td class="text-muted">
                                    <?= $this->escapeHtml($u['cui']) ?>
                                </td>
                                <td>
                                    <?= $this->escapeHtml($u['rol_nombre']) ?>
                                </td>
                                <!-- Estado -->
                                <td>
                                    <?php if ($u['estado'] == 1): ?>
                                        <span class="badge bg-success">Activo</span>
                                    <?php else: ?>
                                        <span class="badge bg-secondary">Inactivo</span>
                                    <?php endif; ?>
                                </td>
                                <td class="text-center">
                                    <div class="d-flex justify-content-center flex-wrap gap-1">
                                        <?php if ($u['estado'] == 1): ?>
                                            <a href="/usuario/editar/<?= $u['id'] ?>"
                                                class="btn btn-sm btn-warning shadow-sm"
                                                title="Editar usuario">
                                                ✏️
                                            </a>
                                            <a href="/admin/usuarios/eliminar/<?= $u['id'] ?>"
                                                class="btn btn-sm btn-danger shadow-sm"
                                                onclick="return confirm('¿Desea eliminar al usuario <?= $this->escapeJs($u['nombre']) ?>?')"
                                                title="Eliminar usuario">
                                                🗑️
                                            </a>
                                        <?php else: ?>
                                            <a href="/admin/usuarios/activar/<?= $u['id'] ?>"
                                                class="btn btn-sm btn-success shadow-sm"
                                                onclick="return confirm('¿Desea activar al usuario <?= $this->escapeJs($u['nombre']) ?>?')"
                                                title="Activar usuario">
                                                ✔ Activar
                                            </a>
                                        <?php endif; ?>
                                    </div>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
        </div>

    <?php endif; ?>
</div>