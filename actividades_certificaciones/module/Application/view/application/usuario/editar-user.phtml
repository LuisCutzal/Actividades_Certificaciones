<div class="container py-5" style="max-width: 720px;">

    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold mb-0">Editar Usuario</h2>

        <?php if ($esAdmin): ?>
            <a href="/admin/listarUsuario" class="btn btn-outline-secondary">
                ⬅ Volver
            </a>
        <?php else: ?>
            <a href="/actividades" class="btn btn-outline-secondary">
                ⬅ Volver a actividades
            </a>
        <?php endif; ?>
    </div>

    <!-- Error -->
    <?php if (!empty($errorMessage)): ?>
        <div class="alert alert-danger shadow-sm rounded">
            <?= $this->escapeHtml($errorMessage) ?>
        </div>
    <?php endif; ?>

    <!-- Card -->
    <div class="card shadow-sm border-0">
        <div class="card-body p-4">

            <form method="post">

                <!-- Datos de acceso -->
                <h5 class="fw-semibold mb-3 text-primary">Datos de acceso</h5>

                <div class="row g-3 mb-4">
                    <!-- Nombre -->
                    <div class="col-md-6">
                        <label class="form-label">Nombre</label>

                        <?php if ($esAdmin): ?>
                            <input
                                type="text"
                                name="nombre"
                                value="<?= $this->escapeHtml($usuario->nombre) ?>"
                                class="form-control"
                                required>
                        <?php else: ?>
                            <input
                                type="text"
                                value="<?= $this->escapeHtml($usuario->nombre) ?>"
                                class="form-control"
                                disabled>
                        <?php endif; ?>

                    </div>

                    <!-- Apellido -->
                    <div class="col-md-6">
                        <label class="form-label">Apellido</label>

                        <?php if ($esAdmin): ?>
                            <input
                                type="text"
                                name="apellido"
                                value="<?= $this->escapeHtml($usuario->apellido) ?>"
                                class="form-control"
                                required>
                        <?php else: ?>
                            <input
                                type="text"
                                value="<?= $this->escapeHtml($usuario->apellido) ?>"
                                class="form-control"
                                disabled>
                        <?php endif; ?>

                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Correo electrónico</label>

                        <?php if ($esAdmin): ?>
                            <input
                                type="email"
                                name="correo"
                                value="<?= $this->escapeHtml($usuario->correo) ?>"
                                class="form-control"
                                required>
                        <?php else: ?>
                            <input
                                type="email"
                                value="<?= $this->escapeHtml($usuario->correo) ?>"
                                class="form-control"
                                disabled>
                        <?php endif; ?>

                    </div>
                </div>

                <div class="row g-3 mb-4">
                    <div class="col-md-6">
                        <label class="form-label">Nueva contraseña</label>
                        <input
                            type="password"
                            name="password"
                            class="form-control"
                            placeholder="Opcional">
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Confirmar nueva contraseña</label>
                        <input
                            type="password"
                            name="password2"
                            class="form-control"
                            placeholder="Opcional">
                    </div>
                </div>

                <!-- Rol (solo admin) -->
                <?php if ($esAdmin): ?>
                    <h5 class="fw-semibold mb-3 text-primary">Rol del usuario</h5>

                    <div class="mb-4">
                        <label class="form-label">Rol</label>

                        <select name="rol_id" class="form-select" required>
                            <option value="">Seleccione un rol</option>

                            <?php foreach ($roles as $id => $nombre): ?>
                                <option
                                    value="<?= $id ?>"
                                    <?= ((int)$usuario->rol_id === (int)$id) ? 'selected' : '' ?>>
                                    <?= $this->escapeHtml($nombre) ?>
                                </option>
                            <?php endforeach; ?>

                        </select>
                    </div>
                <?php endif; ?>



                <!-- Datos personales -->
                <!-- Datos personales -->
                <h5 class="fw-semibold mb-3 text-primary">Datos personales</h5>

                <div class="row g-3 mb-4">

                    <!-- CUI -->
                    <div class="col-md-4">
                        <label class="form-label">CUI</label>

                        <?php if ($esAdmin): ?>
                            <input
                                type="text"
                                name="cui"
                                value="<?= $this->escapeHtml($usuario->cui ?? '') ?>"
                                class="form-control"
                                pattern="[0-9]{13}"
                                maxlength="13"
                                inputmode="numeric"
                                title="El CUI debe contener exactamente 13 dígitos numéricos"
                                oninvalid="this.setCustomValidity('El CUI debe contener exactamente 13 dígitos numéricos')"
                                oninput="this.setCustomValidity('')"
                                required>
                        <?php else: ?>
                            <input
                                type="text"
                                value="<?= $this->escapeHtml($usuario->cui ?? '') ?>"
                                class="form-control"
                                disabled>
                        <?php endif; ?>

                    </div>

                    <!-- Carnet -->
                    <div class="col-md-4">
                        <label class="form-label">Carné</label>

                        <?php if ($esAdmin): ?>
                            <input
                                type="text"
                                name="carnet"
                                value="<?= $this->escapeHtml($usuario->carnet ?? '') ?>"
                                class="form-control"
                                pattern="[0-9]{9}"
                                maxlength="9"
                                inputmode="numeric"
                                title="El carnet debe contener exactamente 9 dígitos numéricos"
                                oninvalid="this.setCustomValidity('El carnet debe contener exactamente 9 dígitos numéricos')"
                                oninput="this.setCustomValidity('')"
                                placeholder="Opcional">
                        <?php else: ?>
                            <input
                                type="text"
                                value="<?= $this->escapeHtml($usuario->carnet ?? '') ?>"
                                class="form-control"
                                disabled>
                        <?php endif; ?>

                    </div>

                    <!-- Registro personal -->
                    <div class="col-md-4">
                        <label class="form-label">Registro personal</label>

                        <?php if ($esAdmin): ?>
                            <input
                                type="text"
                                name="registro_personal"
                                value="<?= $this->escapeHtml($usuario->registro_personal ?? '') ?>"
                                class="form-control"
                                pattern="[0-9]{8}"
                                maxlength="8"
                                inputmode="numeric"
                                title="El registro personal debe contener exactamente 8 dígitos numéricos"
                                oninvalid="this.setCustomValidity('El registro personal debe contener exactamente 8 dígitos numéricos')"
                                oninput="this.setCustomValidity('')"
                                placeholder="Opcional">
                        <?php else: ?>
                            <input
                                type="text"
                                value="<?= $this->escapeHtml($usuario->registro_personal ?? '') ?>"
                                class="form-control"
                                disabled>
                        <?php endif; ?>

                    </div>

                </div>

                <!-- Botón -->
                <div class="d-grid">
                    <button class="btn btn-primary btn-lg shadow-sm">
                        💾 Guardar cambios
                    </button>
                </div>

            </form>

        </div>
    </div>

</div>