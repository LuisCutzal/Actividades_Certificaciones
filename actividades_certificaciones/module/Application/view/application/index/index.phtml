<div class="container" style="margin-top: 80px;">
    <div class="row justify-content-center">
        <div class="col-12 col-sm-10 col-md-6 col-lg-4">
            <div class="shadow p-4 rounded bg-white">
                <h1 class="text-center mb-4">Iniciar sesión</h1>

                <form method="POST" action="/login">
                    <div class="mb-3">
                        <label class="form-label">Registro académico / DPI / Registro personal / Correo</label>
                        <input type="text" name="login" class="form-control" required>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Contraseña</label>
                        <input type="password" name="password" class="form-control" required>
                    </div>

                    <button type="submit" class="btn btn-primary w-100">Ingresar</button>

                    <div class="text-center mt-3">
                        <a href="/forgot-password">¿Olvidaste tu contraseña?</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>


<?php foreach ($this->flashMessenger()->getErrorMessages() as $msg): ?>
    <?= $this->partial('application/index/error', ['errorMessage' => $msg]) ?>
<?php endforeach; ?>

<?php foreach ($this->flashMessenger()->getSuccessMessages() as $msg): ?>
    <?= $this->partial('application/index/success', ['successMessage' => $msg]) ?>
<?php endforeach; ?>
