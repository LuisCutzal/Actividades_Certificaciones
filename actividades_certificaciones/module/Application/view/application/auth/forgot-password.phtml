<?= $this->partial('application/index/success') ?>
<div class="container d-flex justify-content-center align-items-center" style="min-height: 70vh;">
    <div class="card shadow-sm p-4 rounded" style="max-width: 400px; width: 100%;">
        <h2 class="text-center mb-4 fw-bold">Recuperar Contraseña</h2>

        <?php if (!empty($errorMessage)): ?>
            <div class="alert alert-danger shadow-sm">
                <?= $this->escapeHtml($errorMessage) ?>
            </div>
        <?php endif; ?>

        <?php if (!empty($successMessage)): ?>
            <div class="alert alert-success shadow-sm">
                <?= $this->escapeHtml($successMessage) ?>
            </div>
        <?php endif; ?>

        <form method="POST" action="/forgot-password">
            <div class="mb-3">
                <label class="form-label fw-semibold">Correo electrónico</label>
                <input type="email" name="email" class="form-control form-control-lg" placeholder="correo@ejemplo.com" required>
            </div>

            <div class="d-grid mt-3">
                <button type="submit" class="btn btn-primary btn-lg shadow-sm">
                    ✉️ Enviar instrucciones
                </button>
            </div>
        </form>

        <div class="text-center mt-3">
            <a href="/" class="text-decoration-none">&larr; Volver al inicio de sesión</a>
        </div>
    </div>
</div>
