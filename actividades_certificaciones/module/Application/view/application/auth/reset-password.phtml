<div class="container d-flex justify-content-center align-items-center" style="min-height: 70vh;">
    <div class="card shadow-sm p-4 rounded" style="max-width: 400px; width: 100%;">
        <h2 class="text-center mb-4 fw-bold">Restablecer Contraseña</h2>

        <!-- Mensaje de error -->
        <?php if (!empty($this->error)): ?>
            <div class="alert alert-danger shadow-sm">
                <?= $this->escapeHtml($this->error) ?>
            </div>
        <?php endif; ?>

        <form method="POST" action="/reset-password">
            <input type="hidden" name="token" value="<?= $this->escapeHtml($this->token ?? '') ?>">

            <div class="mb-3">
                <label class="form-label fw-semibold">Nueva contraseña</label>
                <input type="password" name="password" class="form-control form-control-lg" placeholder="Ingresa tu nueva contraseña" required>
            </div>

            <div class="mb-3">
                <label class="form-label fw-semibold">Repetir contraseña</label>
                <input type="password" name="password_confirm" class="form-control form-control-lg" placeholder="Repite la contraseña" required>
            </div>

            <div class="d-grid mt-3">
                <button type="submit" class="btn btn-primary btn-lg shadow-sm">
                    🔒 Restablecer contraseña
                </button>
            </div>
        </form>

        <div class="text-center mt-3">
            <a href="/" class="text-decoration-none">&larr; Volver al inicio de sesión</a>
        </div>
    </div>
</div>
