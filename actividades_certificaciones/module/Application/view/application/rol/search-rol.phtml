<div class="container mt-5">

    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h3 class="fw-semibold mb-0">
            Resultados de búsqueda
        </h3>

        <a href="/admin/roles/listar"
            class="btn btn-sm btn-outline-secondary">
            ← Volver al listado
        </a>
    </div>

    <!-- Subtítulo -->
    <p class="text-muted mb-4">
        Coincidencias para:
        <span class="fw-semibold text-primary">
            “<?= $this->escapeHtml($query) ?>”
        </span>
    </p>

    <?php if (empty($roles)): ?>
        <div class="alert alert-light text-center border">
            <span class="text-muted">
                No se encontraron roles con ese criterio.
            </span>
        </div>
    <?php else: ?>

        <!-- Tabla -->
        <div class="card shadow-sm border-0">
            <div class="table-responsive">

                <table class="table table-hover align-middle mb-0">

                    <thead class="table-light small text-uppercase">
                        <tr>
                            <th>ID</th>
                            <th>Nombre del rol</th>
                            <th class="text-center">Acciones</th>
                        </tr>
                    </thead>

                    <tbody>

                        <?php foreach ($roles as $rol): ?>

                            <tr>

                                <td class="fw-medium">
                                    <?= $rol->id ?>
                                </td>

                                <td>
                                    <?= $this->escapeHtml($rol->nombre) ?>
                                </td>

                                <td class="text-center">

                                    <div class="d-flex justify-content-center flex-wrap gap-1">

                                        <a href="/admin/roles/modificar/<?= $rol->id ?>"
                                            class="btn btn-sm btn-warning shadow-sm"
                                            title="Editar rol">
                                            ✏️
                                        </a>

                                        <!-- <a href="/admin/roles/eliminar/<?= $rol->id ?>"
                                            class="btn btn-sm btn-danger shadow-sm"
                                            onclick="return confirm('¿Desea eliminar el rol <?= $this->escapeJs($rol->nombre) ?>?')"
                                            title="Eliminar rol">
                                            🗑️
                                        </a> -->

                                    </div>

                                </td>

                            </tr>

                        <?php endforeach; ?>

                    </tbody>

                </table>

            </div>
        </div>

    <?php endif; ?>

</div>