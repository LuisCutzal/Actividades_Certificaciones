<script src="<?= $this->basePath('js/actividades-form.js') ?>" defer></script>

<div class="container py-5 d-flex justify-content-center">
    <div class="card shadow-sm p-4" style="max-width: 600px; width: 100%;">
        <h3 class="text-center fw-bold mb-4">Definir tipo de participación</h3>

        <form method="post" enctype="multipart/form-data">

            <!-- Tipo de participación -->
            <div class="mb-4">
                <label class="form-label fw-semibold">Tipo de participación</label>

                <div class="d-flex gap-4 mt-2">
                    <div class="form-check">
                        <input class="form-check-input"
                            type="radio"
                            name="tipo_participacion"
                            id="tipo_lista"
                            value="lista"
                            required
                            checked>
                        <label class="form-check-label" for="tipo_lista">
                            Lista de estudiantes (Excel)
                        </label>
                    </div>

                    <div class="form-check">
                        <input class="form-check-input"
                            type="radio"
                            name="tipo_participacion"
                            id="tipo_individual"
                            value="individual">
                        <label class="form-check-label" for="tipo_individual">
                            Estudiante individual
                        </label>
                    </div>
                </div>
            </div>

            <!-- Campo CSV -->
            <div class="mb-3 d-none" id="campo-lista">
                <label class="form-label fw-semibold">Listado de asistencia en "EXCEL"</label>
                <input type="file"
                    name="lista_asistencia"
                    class="form-control form-control-lg"
                    accept="*">
            </div>

            <!-- Campo estudiante individual -->
            <div class="mb-3 d-none" id="campo-individual">
                <label class="form-label fw-semibold">Carné del estudiante</label>
                <input type="text"
                    name="estudiante_individual"
                    class="form-control form-control-lg"
                    placeholder="Ej: 202012345">
            </div>

            <!-- Botones -->
            <div class="d-flex justify-content-end gap-2 mt-4">
                <a href="/actividades" class="btn btn-outline-secondary">
                    Cancelar
                </a>
                <button type="submit" class="btn btn-primary">
                    Guardar
                </button>
            </div>

        </form>
    </div>
</div>