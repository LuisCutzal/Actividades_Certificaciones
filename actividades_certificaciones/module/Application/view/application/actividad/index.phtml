<div class="container py-4">

    <!-- Flash Messages -->
    <?php if ($this->flashMessenger()->hasErrorMessages()): ?>
        <?php foreach ($this->flashMessenger()->getErrorMessages() as $msg): ?>
            <div class="alert alert-warning alert-dismissible fade show shadow-sm" role="alert">
                <?= $this->escapeHtml($msg) ?>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <?php endforeach; ?>
    <?php endif; ?>

    <!-- <script>
    setTimeout(() => {
        document.querySelectorAll('.flash-message').forEach(el => {
            el.classList.remove('show');
            el.classList.add('fade');
            setTimeout(() => el.remove(), 500);
        });
    }, 5000);
    </script> -->

    <!-- Título + botones -->
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center mb-4 gap-3">
        <h2 class="fw-bold mb-0">Actividades</h2>
        <?= $this->partial('application/actividad/partials/botons') ?>
    </div>

    <!-- Filtro de búsqueda -->
    <div class="card mb-4 shadow-sm border-0">
        <div class="card-body p-3">
            <form method="get" action="/actividades" class="row g-2 align-items-center">
                <div class="col-md-8">
                    <input type="text" name="buscar" class="form-control form-control-lg"
                        placeholder="Nombre, organizador o crédito exacto (ej: 2.5)"
                        value="<?= $this->escapeHtml($this->buscar ?? '') ?>">
                </div>
                <div class="col-md-4 d-flex gap-2">
                    <button class="btn btn-primary w-100 btn-lg">🔍 Buscar</button>
                    <a href="/actividades" class="btn btn-outline-secondary w-100 btn-lg">Limpiar</a>
                </div>
            </form>
        </div>
    </div>

    <!-- Alertas de pendientes -->
    <?php if ($this->esPendientes === true): ?>
        <div class="alert <?= count($this->actividades) === 0 ? 'alert-info' : 'alert-danger' ?> shadow-sm rounded">
            <?= count($this->actividades) === 0
                ? 'No existen actividades pendientes por APROBAR.'
                : 'Existen actividades pendientes por APROBAR.' ?>
        </div>
    <?php elseif (!$this->esPendientes && $this->hayPendientes): ?>
        <div class="alert alert-danger shadow-sm rounded">
            Existen actividades pendientes por APROBAR.
        </div>
    <?php else: ?>
        <div class="alert alert-info shadow-sm rounded">
            No existen actividades pendientes por APROBAR.
        </div>
    <?php endif; ?>

    <!-- Tabla de actividades -->
    <div class="table-responsive shadow-sm rounded">
        <table class="table table-hover align-middle mb-0">
            <thead class="table-light text-uppercase small">
                <tr>
                    <th>Nombre</th>
                    <th>Fecha</th>
                    <th>Créditos</th>
                    <th>Organizador</th>
                    <th class="text-center">Acciones</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($this->actividades as $actividad): ?>
                    <tr>
                        <td class="fw-semibold"><?= $this->escapeHtml($actividad->nombre) ?></td>
                        <td><?= date('d/m/Y', strtotime($actividad->fecha)) ?></td>
                        <td><?= $actividad->credito ?></td>
                        <td style="max-width: 180px; word-wrap: break-word;">
                            <?= $this->escapeHtml($actividad->organizador_nombre ?? '—') ?>
                        </td>
                        <td class="text-center align-middle">
                            <?= $this->partial('application/actividad/partials/botones-acciones', [
                                'actividad' => $actividad,
                                'tipo_id' => $this->tipo_id
                            ]) ?>
                        </td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>

        <!-- Paginación -->
        <?= $this->partial('application/partials/pagination', [
            'paginator' => $actividades,
            'query'     => [
                'buscar' => $buscar ?? ''
            ]
        ]) ?>
    </div>

</div>