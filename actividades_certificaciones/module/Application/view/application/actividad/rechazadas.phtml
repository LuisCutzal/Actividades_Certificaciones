<div class="container py-4">

    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold mb-0 text-danger">
            ✖ Actividades no realizadas
        </h2>

        <a href="/actividades" class="btn btn-outline-secondary">
            ← Volver
        </a>
    </div>

    <!-- Filtro de búsqueda -->
    <div class="card mb-4 shadow-sm border-0">
        <div class="card-body p-3">
            <form method="get" class="row g-2 align-items-center">
                <div class="col-md-8">
                    <input
                        type="text"
                        name="buscar"
                        class="form-control form-control-lg"
                        placeholder="Buscar por actividad, organizador o motivo"
                        value="<?= $this->escapeHtml($this->buscar ?? '') ?>">
                </div>
                <div class="col-md-4 d-flex gap-2">
                    <button class="btn btn-danger w-100 btn-lg">🔍 Buscar</button>
                    <a href="/actividades/rechazadas"
                        class="btn btn-outline-secondary w-100 btn-lg">
                        Limpiar
                    </a>
                </div>
            </form>
        </div>
    </div>


    <!-- Tabla -->
    <div class="card shadow-sm">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-light text-uppercase small">
                        <tr>
                            <th>Actividad</th>
                            <th>Organizador</th> <!-- Nueva columna -->
                            <th class="text-center">Fecha de rechazo</th>
                            <th>Motivo</th>
                            <th class="text-center">Acción</th>
                        </tr>
                    </thead>

                    <tbody>
                        <?php if (empty($actividades)): ?>
                            <tr>
                                <td colspan="5" class="text-center text-muted py-4">
                                    No existen actividades no realizadas.
                                </td>
                            </tr>
                        <?php else: ?>
                            <?php foreach ($actividades ?? [] as $actividad): ?>
                                <tr>
                                    <td class="fw-semibold">
                                        <?= $this->escapeHtml($actividad->nombre) ?>
                                    </td>

                                    <td>
                                        <?= $this->escapeHtml(
                                            $roles[$actividad->organizador] ?? 'Desconocido'
                                        ) ?>
                                    </td>

                                    <td class="text-center">
                                        <?= date('d/m/Y', strtotime($actividad->fecha_resolucion)) ?>
                                    </td>

                                    <td class="text-muted">
                                        <?= $this->escapeHtml(
                                            mb_strimwidth($actividad->motivo_rechazo, 0, 60, '...')
                                        ) ?>
                                    </td>

                                    <td class="text-center">
                                        <a href="/actividades/ver-rechazo/<?= $actividad->id ?>"
                                            class="btn btn-sm btn-outline-danger">
                                            👁 Ver detalle
                                        </a>
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                        <?php endif; ?>
                    </tbody>
                </table>

                <!-- Paginación -->
                <?= $this->partial('application/partials/pagination', [
                    'paginator' => $actividades
                ]) ?>

            </div>

        </div>
    </div>

</div>