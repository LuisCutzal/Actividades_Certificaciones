<div class="container py-4">

    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="fw-bold mb-0">Lista de Asistencia</h2>
        <a href="/actividades" class="btn btn-outline-secondary">
            ⬅ Volver a actividades
        </a>
    </div>

    <?php if ((int)$this->actividadEstado === 2): ?>
        <div class="alert alert-danger d-flex align-items-center gap-2">
            <strong>Actividad no realizada.</strong>
            <a href="/actividades/ver-rechazo/<?= $this->actividadId ?>"
                class="btn btn-sm btn-outline-danger ms-auto">
                Ver motivo
            </a>
        </div>
    <?php endif; ?>

    <!-- Buscador -->
    <div class="card mb-3 shadow-sm border-0">
        <div class="card-body p-3">
            <form method="get" class="row g-2 align-items-stretch">
                <div class="col-md-8">
                    <input type="text"
                        name="buscar"
                        class="form-control form-control-lg"
                        placeholder="Buscar por nombre o carné"
                        value="<?= $this->escapeHtml($this->buscar ?? '') ?>">
                </div>

                <div class="col-md-4 d-flex gap-2">
                    <button class="btn btn-primary btn-lg flex-fill">
                        🔍 Buscar
                    </button>

                    <a href="/actividades/<?= $this->actividadId ?>/asistencia"
                        class="btn btn-outline-secondary btn-lg flex-fill">
                        Limpiar
                    </a>
                </div>
            </form>
        </div>
    </div>

    <!-- Card de tabla -->
    <div class="card shadow-sm">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-striped table-hover mb-0 align-middle">
                    <thead class="table-light text-uppercase small">
                        <tr>
                            <th class="text-center">ID Estudiante</th>
                            <th class="text-center">Carné</th>
                            <th class="text-center">Nombre completo</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php if (empty($this->estudiantes)): ?>
                            <tr>
                                <td colspan="3" class="text-center text-muted py-3">
                                    No se detectaron estudiantes en esta lista.
                                </td>
                            </tr>
                        <?php else: ?>
                            <?php foreach ($this->estudiantes as $alumno): ?>
                                <tr>
                                    <td class="text-center"><?= $alumno['carnet'] ?></td>
                                    <td class="text-center"><?= $alumno['carnet'] ?></td>
                                    <td class="text-center"><?= $alumno['nombre'] ?></td>
                                </tr>
                            <?php endforeach; ?>
                        <?php endif; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <!-- Acciones -->
    <?php if (isset($this->listaEstado)): ?>
        <div class="mt-3 d-flex justify-content-end align-items-center gap-2">

            <?php if ((int)$this->actividadEstado === 2): ?>
                <!-- ACTIVIDAD RECHAZADA -->
                <span class="badge bg-danger fs-6 px-3 py-2 shadow-sm">
                    ✖ Actividad no realizada
                </span>

            <?php elseif ($this->listaEstado == 0): ?>
                <!-- LISTA PENDIENTE -->
                <?php if ($this->tipo_id === 3): ?>
                    <a href="/actividades/aprobar/<?= $this->actividadId ?>"
                        onclick="return confirm('¿Aprobar esta lista?')"
                        class="btn btn-success btn-lg shadow-sm d-flex align-items-center gap-2">
                        <span class="fs-5">✔</span>
                        <span>Aprobar</span>
                    </a>
                <?php endif; ?>

            <?php else: ?>
                <!-- LISTA APROBADA -->
                <span class="badge bg-success fs-6 px-3 py-2 shadow-sm d-flex align-items-center gap-1">
                    <span class="fs-5">✔</span>
                    <span>Aprobado</span>
                </span>
            <?php endif; ?>

        </div>
    <?php endif; ?>


</div>